{
  "name": "cqumirror_mail",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "postal-mime": "^2.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}