{
  "name": "cqumirror_mail",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "postal-mime": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^4.90.1"
  }
}
